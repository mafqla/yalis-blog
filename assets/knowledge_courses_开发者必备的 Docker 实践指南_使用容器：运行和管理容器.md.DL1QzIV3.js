import{_ as i}from"./chunks/ArticleMetadata.BPGENPYh.js";import{_ as r,B as l,o as p,c as h,j as a,a as t,E as u,w as k,b as g,e as m,a4 as b}from"./chunks/framework.CW-6O5Zu.js";import"./chunks/theme.CV6NJBc4.js";const T=JSON.parse('{"title":"运行和管理容器","description":"","frontmatter":{},"headers":[],"relativePath":"knowledge/courses/开发者必备的 Docker 实践指南/使用容器：运行和管理容器.md","filePath":"knowledge/courses/开发者必备的 Docker 实践指南/使用容器：运行和管理容器.md","lastUpdated":1776395729000}'),v={name:"knowledge/courses/开发者必备的 Docker 实践指南/使用容器：运行和管理容器.md"};function x(o,e,C,D,_,f){const c=i,d=l("ClientOnly");return p(),h("div",null,[e[0]||(e[0]=a("h3",{id:"本资源由-itjc8-com-收集整理",tabindex:"-1"},[t("本资源由 itjc8.com 收集整理 "),a("a",{class:"header-anchor",href:"#本资源由-itjc8-com-收集整理","aria-label":'Permalink to "本资源由 itjc8.com 收集整理"'},"​")],-1)),e[1]||(e[1]=a("h1",{id:"运行和管理容器",tabindex:"-1"},[t("运行和管理容器 "),a("a",{class:"header-anchor",href:"#运行和管理容器","aria-label":'Permalink to "运行和管理容器"'},"​")],-1)),u(d,null,{default:k(()=>{var s,n;return[(((s=o.$frontmatter)==null?void 0:s.aside)??!0)&&(((n=o.$frontmatter)==null?void 0:n.showArticleMetadata)??!0)?(p(),g(c,{key:0,article:o.$frontmatter,readTime:4,words:727},null,8,["article"])):m("",!0)]}),_:1}),e[2]||(e[2]=b(`<p>容器是基于容器技术所建立和运行的轻量级应用运行环境，它是 Docker 封装和管理应用程序或微服务的“集装箱”。在 Docker 中，容器算是最核心的部分了，掌握容器的操作也是 Docker 中最基础的技能了。在这一节中，我们会深入了解容器，展示关于容器的操作。</p><h2 id="容器的创建和启动" tabindex="-1">容器的创建和启动 <a class="header-anchor" href="#容器的创建和启动" aria-label="Permalink to &quot;容器的创建和启动&quot;">​</a></h2><p>在了解容器的各项操作之前，我们再来回顾一下之前我们所提及的容器状态流转。</p><p><img src="https://user-gold-cdn.xitu.io/2018/9/17/165e53743e730432?w=1829&amp;h=932&amp;f=png&amp;s=215394" alt="" loading="lazy"></p><p>在这幅图中，我们可以看到，Docker 容器的生命周期里分为五种状态，其分别代表着：</p><ul><li><strong>Created</strong>：容器已经被创建，容器所需的相关资源已经准备就绪，但容器中的程序还未处于运行状态。</li><li><strong>Running</strong>：容器正在运行，也就是容器中的应用正在运行。</li><li><strong>Paused</strong>：容器已暂停，表示容器中的所有程序都处于暂停 ( 不是停止 ) 状态。</li><li><strong>Stopped</strong>：容器处于停止状态，占用的资源和沙盒环境都依然存在，只是容器中的应用程序均已停止。</li><li><strong>Deleted</strong>：容器已删除，相关占用的资源及存储在 Docker 中的管理信息也都已释放和移除。</li></ul><h3 id="创建容器" tabindex="-1">创建容器 <a class="header-anchor" href="#创建容器" aria-label="Permalink to &quot;创建容器&quot;">​</a></h3><p>当我们选择好镜像以后，就可以通过 <code>docker create</code> 这个命令来创建容器了。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker create nginx:1.12</span></span>
<span class="line"><span>34f277e22be252b51d204acbb32ce21181df86520de0c337a835de6932ca06c3</span></span></code></pre></div><p>执行 <code>docker create</code> 后，Docker 会根据我们所给出的镜像创建容器，在控制台中会打印出 Docker 为容器所分配的容器 ID，此时容器是处于 Created 状态的。</p><p>之后我们对容器的操作可以通过这个容器 ID 或者它的缩略形式进行，但用容器 ID 操作容器就和用镜像 ID 操作镜像一样烦闷，所以我们更习惯于使用容器名来操作容器。</p><p>要使用容器名操作容器，就先得给容器命名，在创建容器时，我们可以通过 <code>--name</code> 这个选项来配置容器名。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker create --name nginx nginx:1.12</span></span></code></pre></div><h3 id="启动容器" tabindex="-1">启动容器 <a class="header-anchor" href="#启动容器" aria-label="Permalink to &quot;启动容器&quot;">​</a></h3><p>通过 <code>docker create</code> 创建的容器，是处于 Created 状态的，其内部的应用程序还没有启动，所以我们需要通过 <code>docker start</code> 命令来启动它。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker start nginx</span></span></code></pre></div><p>由于我们为容器指定了名称，这样的操作会更加自然，所以我们非常推荐为每个被创建的容器都进行命名。</p><p>当容器启动后，其中的应用就会运行起来，容器的几个生命周期也会绑定到了这个应用上，这个之前我们已经提及，这里就不在赘述。只要应用程序还在运行，那么容器的状态就会是 Running，除非进行一些修改容器的操作。</p><p>在 Docker 里，还允许我们通过 <code>docker run</code> 这个命令将 <code>docker create</code> 和 <code>docker start</code> 这两步操作合成为一步，进一步提高工作效率。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker run --name nginx -d nginx:1.12</span></span>
<span class="line"><span>89f2b769498a50f5c35a314ab82300ce9945cbb69da9cda4b022646125db8ca7</span></span></code></pre></div><p>通过 <code>docker run</code> 创建的容器，在创建完成之后会直接启动起来，不需要我们再使用 <code>docker start</code> 去启动了。</p><p>这里需要注意的一点是，通常来说我们启动容器会期望它运行在“后台”，而 <code>docker run</code> 在启动容器时，会采用“前台”运行这种方式，这时候我们的控制台就会衔接到容器上，不能再进行其他操作了。我们可以通过 <code>-d</code> 或 <code>--detach</code> 这个选项告诉 Docker 在启动后将程序与控制台分离，使其进入“后台”运行。</p><h2 id="管理容器" tabindex="-1">管理容器 <a class="header-anchor" href="#管理容器" aria-label="Permalink to &quot;管理容器&quot;">​</a></h2><p>容器创建和启动后，除了关注应用程序是否功能正常外，我们也会关注容器的状态等内容。</p><p>通过 <code>docker ps</code> 这个命令，我们可以罗列出 Docker 中的容器。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker ps</span></span>
<span class="line"><span>CONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS              PORTS               NAMES</span></span>
<span class="line"><span>89f2b769498a        nginx:1.12          &quot;nginx -g &#39;daemon of…&quot;   About an hour ago   Up About an hour    80/tcp              nginx</span></span></code></pre></div><p>默认情况下，<code>docker ps</code> 列出的容器是处于运行中的容器，如果要列出所有状态的容器，需要增加 <code>-a</code> 或 <code>--all</code> 选项。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker ps -a</span></span>
<span class="line"><span>CONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS              PORTS               NAMES</span></span>
<span class="line"><span>425a0d3cd18b        redis:3.2           &quot;docker-entrypoint.s…&quot;   2 minutes ago       Created                                 redis</span></span>
<span class="line"><span>89f2b769498a        nginx:1.12          &quot;nginx -g &#39;daemon of…&quot;   About an hour ago   Up About an hour    80/tcp              nginx</span></span></code></pre></div><p>在 <code>docker ps</code> 的结果中，我们可以看到几项关于容器的信息。其中 <strong>CONTAINER ID</strong>、<strong>IMAGE</strong>、<strong>CREATED</strong>、<strong>NAMES</strong> 大家都比较容易理解，分别表示容器 ID，容器所基于的镜像，容器的创建时间和容器的名称。</p><p>结果中的 <strong>COMMAND</strong> 表示的是容器中主程序 ( 也就是与容器生命周期所绑定进程所关联的程序 ) 的启动命令，这条命令是在镜像内定义的，而容器的启动其实质就是启动这条命令。关于 COMMAND 的更多知识，我们在之后的 Docker 镜像制作中会更详细的解读。</p><p>结果中的 <strong>STATUS</strong> 表示容器所处的状态，其值和我们之前所谈到的状态有所区别，主要是因为这里还记录了其他的一些信息。在这里，常见的状态表示有三种：</p><ul><li><strong>Created</strong> 此时容器已创建，但还没有被启动过。</li><li><strong>Up [ Time ]</strong> 这时候容器处于正在运行状态，而这里的 Time 表示容器从开始运行到查看时的时间。</li><li><strong>Exited ([ Code ]) [ Time ]</strong> 容器已经结束运行，这里的 Code 表示容器结束运行时，主程序返回的程序退出码，而 Time 则表示容器结束到查看时的时间。</li></ul><p>有些读者有疑问，既然是列出容器，应该为命令取一些带有 <code>ls</code> 字眼的名字，为啥会用类似 Linux 中查看进程的 <code>ps</code> 呢？这其实有一部分历史原因，由于容器并非真的包裹住了进程，而只是隔离了进程，进程还是允许在宿主机操作系统之上的，所以列出镜像的过程到更新是查看正在运行的进程，故而有了这样的名字。</p><p>当然，在 Docker 逐渐成熟后，命令的命名也没有原来那么随意了，已经逐渐转换为使用大家广泛认可的形式。只是 <code>docker ps</code> 这条命令，还保留着复古的风格。</p><h3 id="停止和删除容器" tabindex="-1">停止和删除容器 <a class="header-anchor" href="#停止和删除容器" aria-label="Permalink to &quot;停止和删除容器&quot;">​</a></h3><p>要将正在运行的容器停止，我们可以使用 <code>docker stop</code> 命令。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker stop nginx</span></span></code></pre></div><p>容器停止后，其维持的文件系统沙盒环境还是存在的，内部被修改的内容也都会保留，我们可以通过 <code>docker start</code> 命令将这个容器再次启动。</p><p>当我们需要完全删除容器时，可以通过 <code>docker rm</code> 命令将容器进行删除。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker rm nginx</span></span></code></pre></div><p>正在运行中的容器默认情况下是不能被删除的，我们可以通过增加 <code>-f</code> 或 <code>--force</code> 选项来让 <code>docker rm</code> 强制停止并删除容器，不过这种做法并不妥当。</p><h3 id="随手删除容器" tabindex="-1">随手删除容器 <a class="header-anchor" href="#随手删除容器" aria-label="Permalink to &quot;随手删除容器&quot;">​</a></h3><p>与其他虚拟机不同，Docker 的轻量级容器设计，讲究随用随开，随关随删。也就是说，当我们短时间内不需要使用容器时，最佳的做法是删除它而不是仅仅停止它。</p><p>有的读者会问，容器一旦删除，其内部的文件系统变动也就消失了，这样做岂不是非常麻烦。要解决这个疑惑，其根本是解决为什么我们会对容器中的文件系统做更改。我这里总结了两个对虚拟环境做更改的原因，以及在 Docker 中如何优雅的解决它们。</p><ul><li><p>在使用虚拟机或其他虚拟化所搭建的虚拟环境时，我们倾向于使用一个干净的系统镜像并搭建程序的运行环境，由于将这类虚拟环境制作成镜像的成本较高，耗时也非常久，所以我们对于一些细小的改动倾向于修改后保持虚拟环境不被清除即可。而在 Docker 中，打包镜像的成本是非常低的，其速度也快得惊人，所以如果我们要为程序准备一些环境或者配置，完全可以直接将它们打包至新的镜像中，下次直接使用这个新的镜像创建容器即可。</p></li><li><p>容器中应用程序所产生的一些文件数据，是非常重要的，如果这些数据随着容器的删除而丢失，其损失是非常巨大的。对于这类由应用程序所产生的数据，并且需要保证它们不会随着容器的删除而消失的，我们可以使用 Docker 中的数据卷来单独存放。由于数据卷是独立于容器存在的，所以其能保证数据不会随着容器的删除而丢失。关于数据卷的具体使用，在之后的小节会专门讲解。</p></li></ul><p>解决了这两个问题，大家心中的疑虑是不是就小了很多。而事实上，容器的随用随删既能保证在我们不需要它们的时候它们不会枉占很多资源，也保证了每次我们建立和启动容器时，它们都是“热乎”的崭新版本。大家都知道，系统卡就重装，而借助 Docker 秒级的容器启停特性，我们就是可以这么任性的“重装”。</p><h2 id="进入容器" tabindex="-1">进入容器 <a class="header-anchor" href="#进入容器" aria-label="Permalink to &quot;进入容器&quot;">​</a></h2><p>很多时间，我们需要的操作并不仅仅是按镜像所给出的命令启动容器而已，我们还会希望进一步了解容器或操作容器，这时候最佳的方式就是让我们进入到容器了。</p><p>我们知道，容器是一个隔离运行环境的东西，它里面除了镜像所规定的主进程外，其他的进程也是能够运行的，Docker 为我们提供了一个命令 <code>docker exec</code> 来让容器运行我们所给出的命令。</p><p>这里我们试试用容器中的 <code>more</code> 命令查看容器的主机名定义。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker exec nginx more /etc/hostname</span></span>
<span class="line"><span>::::::::::::::</span></span>
<span class="line"><span>/etc/hostname</span></span>
<span class="line"><span>::::::::::::::</span></span>
<span class="line"><span>83821ea220ed</span></span></code></pre></div><p><code>docker exec</code> 命令能帮助我们在正在运行的容器中运行指定命令，这对于服务控制，运维监控等有着不错的应用场景。但是在开发过程中，我们更常使用它来作为我们进入容器的桥梁。</p><p>熟悉 Linux 的朋友们知道，我们操作 Linux 这个过程，并不是 Linux 内部的某些机能，而是通过控制台软件来完成的。控制台软件分析我们的命令，将其转化为对 Linux 的系统调用，实现了我们对 Linux 的操作。若不是这样，生涩的系统调用方法对普通开发者来说简直就是黑洞一般的存在，更别提用它们控制系统了。</p><p>在 Linux 中，大家熟悉的控制台软件应该是 Shell 和 Bash 了，它们分别由 sh 和 bash 这两个程序启动。</p><p>说到这里，有读者一定想到了，既然有这两个控制台程序，我们只要在容器里执行它们，然后通过它们去控制容器内的环境，岂不就可以“自由的飞翔”了吗。没错，这里说的进入容器，就是通过 <code>docker exec</code> 命令来启动 sh 或 bash，并通过它们实现对容器内的虚拟环境的控制。</p><p>由于 bash 的功能要比 sh 丰富，所以在能够使用 bash 的容器里，我们优先选择它作为控制台程序。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker exec -it nginx bash</span></span>
<span class="line"><span>root@83821ea220ed:/#</span></span></code></pre></div><p>在借助 <code>docker exec</code> 进入容器的时候，我们需要特别注意命令中的两个选项不可或缺，即 <code>-i</code> 和 <code>-t</code> ( 它们俩可以利用简写机制合并成 <code>-it</code> )。</p><p>其中 <code>-i</code> ( <code>--interactive</code> ) 表示保持我们的输入流，只有使用它才能保证控制台程序能够正确识别我们的命令。而 <code>-t</code> ( <code>--tty</code> ) 表示启用一个伪终端，形成我们与 bash 的交互，如果没有它，我们无法看到 bash 内部的执行结果。</p><p>熟悉通过在容器中执行控制台程序进而进入容器这种方法，在开发过程中你能更轻松的观察容器中发生了什么，也更容易排查程序或者环境引起的问题。</p><h3 id="衔接到容器" tabindex="-1">衔接到容器 <a class="header-anchor" href="#衔接到容器" aria-label="Permalink to &quot;衔接到容器&quot;">​</a></h3><p>Docker 为我们提供了一个 <code>docker attach</code> 命令，用于将当前的输入输出流连接到指定的容器上。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes one-dark-pro material-theme-palenight vp-code" tabindex="0"><code><span class="line"><span>$ sudo docker attach nginx</span></span></code></pre></div><p>这个命令最直观的效果可以理解为我们将容器中的主程序转为了“前台”运行 ( 与 <code>docker run</code> 中的 <code>-d</code> 选项有相反的意思 )。</p><p>由于我们的输入输出流衔接到了容器的主程序上，我们的输入输出操作也就直接针对了这个程序，而我们发送的 Linux 信号也会转移到这个程序上。例如我们可以通过 Ctrl + C 来向程序发送停止信号，让程序停止 ( 从而容器也会随之停止 )。</p><p>在实际开发中，由于 <code>docker attach</code> 限制较多，功能也不够强大，所以并没有太多用武之地，这里我们就一笔带过，不做详细的解读了。</p><h2 id="留言互动" tabindex="-1">留言互动 <a class="header-anchor" href="#留言互动" aria-label="Permalink to &quot;留言互动&quot;">​</a></h2><p>在本节中，我们对容器相关的操作进行了深入的了解，对容器的运行状态及相关知识做了介绍。这里给大家留一道实践题：</p><blockquote><p>试着创建 Nginx 容器，之后进入到容器中，利用相关命令停止 Nginx 程序的运行，观察操作之后容器的状态变化。</p></blockquote><p>欢迎大家通过留言的方式说出你的实践之路。我会选出有代表性的优质留言，推荐给大家。</p><p>同时，如果你对容器相关的操作与使用还有什么不理解的地方，或者对其有独特的见解，可以加入到这本小册的官方微信群中，参与对相关问题的讨论。</p>`,71))])}const N=r(v,[["render",x]]);export{T as __pageData,N as default};
